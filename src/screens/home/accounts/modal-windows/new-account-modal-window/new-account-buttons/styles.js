import { SCREEN_WIDTH } from '../../../../../../constants/variableConstants'

export const styles = {
	newAccountButtons: {
		alignItems: 'center'

	},
	newAccountButtonsWrapper: {
		width: SCREEN_WIDTH - 20,
		alignItems: 'flex-end'
	}
}
import { SCREEN_WIDTH } from '../../../constants/variableConstants'

export const styles = {
    filterRangeValue: {
        maxWidth: SCREEN_WIDTH * 0.4,

        fontWeight: 600,

        overflow: 'hidden',
    },
}